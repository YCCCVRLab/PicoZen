# This workflow has been consolidated into android.yml
# The android.yml workflow handles:
# - Building the APK
# - Uploading as artifacts
# - Using updated actions (v4 instead of deprecated v1)
#
# This file is kept for reference but the workflow is disabled
# by removing the trigger conditions.

# name: Build PicoZen APK
# on:
#   push:
#     branches: [ main ]
#   workflow_dispatch:

# Workflow disabled - use android.yml instead